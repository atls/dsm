query GetOpenIssues($id: String!) {
  node(id: $id) {
    __typename
    ... on Repository {
      issues(first: 100, states: OPEN) {
        nodes {
          id
          number
          title
        }
      }
    }
  }
}